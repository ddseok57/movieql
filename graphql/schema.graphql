type Movie {
    id: Int!
    name: String!
    score: ID!
}

type Query {
    movies: [Movie]!
    movie(id: ID!): Movie
}

type Mutation {
    addMovie(name: String!, score: Int!): Movie!
    deleteMovie(id: ID!): Boolean!
}